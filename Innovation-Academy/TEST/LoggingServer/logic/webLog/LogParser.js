/* Weblog를 parsing해서 객체를 생성 */
const regex = require("../../regex/webLogRegex");

exports.WebLogParser = (LogArray) =>{
    const objectWebLog = {ip, url, data, method};
    LogArray.forEach(element => {
        
    });

};